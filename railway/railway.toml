[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "ald"
source = "."

[services.variables]
TENANT_SLUG = "ald"
NODE_ENV = "production"
PORT = "3000"
